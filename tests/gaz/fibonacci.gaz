(let a 0)
(let b 1)
(let tmp 0)
(let iters 20)
(
  while iters (
    (let iters (- iters 1))

    (printf "%4d: a: %6d, b: %6d\n" iters a b)

    (let tmp b)
    (let b (+ a b))
    (let a tmp)
  )
)
(printf "\n20th Fibonacci number is: %d\n" tmp)
